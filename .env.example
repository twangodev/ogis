# Log level: error, warn, info, debug, trace
# Use debug for development, info for production
RUST_LOG=debug

# Server address to bind to (optional, defaults to 0.0.0.0:3000). This can also be set via --addr CLI flag
# OGIS_ADDR=<host>:<port>
