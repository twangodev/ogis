apiVersion: v1
kind: Service
metadata:
  name: {{ include "ogis.fullname" . }}
  labels:
    {{- include "ogis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ogis.selectorLabels" . | nindent 4 }}
