apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ogis.fullname" . }}-test-connection"
  labels:
    {{- include "ogis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ogis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
